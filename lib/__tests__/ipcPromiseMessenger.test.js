const assert = require('assert');
const ipcPromiseMessenger = require('../index.js');

describe('ipcPromiseMessenger', function () {
  it('has a test', function () {
    assert(false, 'ipcPromiseMessenger should have a test');
  });
});
